import{auth,db,createUserWithEmailAndPassword,signInWithEmailAndPassword,updateProfile,ref,set}from"./firebase.js";import{header,footer}from"./ui.js";header();footer();const msg=document.getElementById("form-msg");document.getElementById("login-form")?.addEventListener("submit",async e=>{e.preventDefault();msg.textContent="Signing in…";try{await signInWithEmailAndPassword(auth,email.value.trim(),password.value);location.href="dashboard.html"}catch(x){msg.textContent=x.message}});document.getElementById("register-form")?.addEventListener("submit",async e=>{e.preventDefault();msg.textContent="Creating account…";try{const c=await createUserWithEmailAndPassword(auth,email.value.trim(),password.value);await updateProfile(c.user,{displayName:name.value.trim()});await set(ref(db,"users/"+c.user.uid),{uid:c.user.uid,name:name.value.trim(),email:c.user.email,createdAt:Date.now()});location.href="dashboard.html"}catch(x){msg.textContent=x.message}});
